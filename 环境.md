# 环境安装与完整工作区复现

本文说明如何从 GitHub 克隆后，还原为本项目当前的工作区布局。仓库只保存源码、文档、小型示例和安装脚本；Python 环境、PSD2Live 便携包、See-through 模型权重及生成结果均在本机重建。

## 1. 克隆

```bat
git clone https://github.com/linnnn89/live2d-maker.git
cd live2d-maker
```

`see-through/` 与 `psd2live/` 的源码已经直接包含在仓库中，不需要额外 submodule 命令。

## 2. 最快安装（Windows）

先安装：

- Git for Windows
- Python 3.10 x64（需要 `py -3.10` 可用）
- 若运行 See-through：Miniconda 或 Miniforge、NVIDIA 驱动（默认脚本按 CUDA 12.8）
- 若从源码构建 PSD2Live：JDK 21
- 若运行 viewer：Microsoft Edge（Playwright 使用系统 Edge channel）

### 基础工作区

创建 `python/` 辅助环境并下载 PSD2Live v0.7.1 便携版：

```bat
setup-windows.bat
```

### 加 See-through 运行环境

```bat
setup-windows.bat -WithSeeThrough
```

### 完整安装，包括模型权重

> 该步骤会从 Hugging Face 下载约数十 GB 数据。请预留磁盘空间和时间。

```bat
setup-windows.bat -WithSeeThrough -WithModels
```

仅预览脚本将执行什么，不写磁盘：

```bat
setup-windows.bat -DryRun -WithSeeThrough -WithModels
```

## 3. 安装后的目录

```text
live2d-maker/
├─ psd2live/                 # GPL-3.0 源码（已在 Git）
├─ see-through/              # Apache-2.0 源码（已在 Git）
│  ├─ .hf_home/              # Hugging Face 权重缓存（下载生成，不入 Git）
│  └─ workspace/             # 拆层结果/数据集（不入 Git）
├─ python/                   # Python 3.10 辅助 venv（生成，不入 Git）
├─ portable/PSD2Live/        # v0.7.1 便携版（下载生成，不入 Git）
├─ live2d-viewer/            # 参数化渲染 harness
└─ skills/live2d-studio/     # Agent 路由技能包
```

## 4. 精确版本基线

| 项目 | 版本/基线 |
|---|---|
| PSD2Live 源码 | `0.7.1` 系列，Gradle 文件中的 `version = 0.7.1` |
| PSD2Live portable | 上游 Release `v0.7.1` |
| See-through | 上游 commit `7f139bb25c46a0c8ac720d95ddab185fcda5451c` |
| See-through Python | 3.12 |
| See-through PyTorch | 2.8.0 + CUDA 12.8 |
| 辅助 Python | 3.10 |
| PSD2Live 源码构建 | JDK 21、Gradle wrapper 9.6.1 |
| Node（当前验证机） | 22.23.1 |

AMD/ROCm、Linux 和 Apple Silicon 用户应遵循 `see-through/README.md` 选择对应 PyTorch；一键脚本只针对 Windows + NVIDIA CUDA 12.8。

## 5. 模型来源

基础拆层流程会使用：

- <https://huggingface.co/layerdifforg/seethroughv0.0.2_layerdiff3d>
- <https://huggingface.co/24yearsold/seethroughv0.0.1_marigold>
- <https://huggingface.co/24yearsold/l2d_sam_iter2>

See-through 还可能按所选 annotator 自动下载额外模型。所有 Hugging Face 缓存统一放在 `see-through/.hf_home/`，根 `.gitignore` 会阻止它进入 Git。

## 6. 运行

### PSD2Live 便携版

```powershell
.\start-psd2live.ps1
```

### PSD2Live 源码

```bat
cd psd2live
gradlew.bat run
```

### See-through

```bat
set HF_HOME=%CD%\see-through\.hf_home
conda run -n see_through_dev python see-through\inference\scripts\inference_psd.py --srcp input.png --save_to_psd
```

生成结果默认位于 `see-through/workspace/`，不入 Git。

### 渲染验证

```bat
python\Scripts\python.exe live2d-viewer\shot.py --help
```

## 7. 自检

```bat
python\Scripts\python.exe -c "import jpype,numpy,scipy,PIL,psd_tools,playwright; print('tools OK')"
python skills\live2d-studio\scripts\check_routing.py
conda run -n see_through_dev python -c "import torch,diffusers,transformers; print(torch.__version__)"
```

## 8. 为什么没有把大文件上传 GitHub

以下内容可重复下载或重新生成，上传会让仓库膨胀到数十 GB：

- `see-through/.hf_home/`：模型权重缓存，当前机器约 26GB；
- `see-through/.venv/`、根 `python/`：机器相关虚拟环境；
- `portable/PSD2Live/`：上游 Release 已提供；
- `workspace/`、`out/`、构建目录和渲染结果；
- `.safetensors`、`.pt`、`.ckpt`、`.bin`、`.onnx` 等权重文件。

源码、requirements、Gradle wrapper、许可证、下载地址和固定版本均保留，因此读者可重建同等工作区。
